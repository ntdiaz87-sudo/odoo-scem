<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- SNIPPET: solar_calculator
         NOTA CLAUDE CODE: portar el bloque 'solar_calculator' de preview/index.html
         a este snippet, con la MISMA estructura que snippets/hero_split.xml:
           1. <template id="s_cevende_solar_calculator"> con el HTML del bloque.
           2. Registro en website.snippets para hacerlo arrastrable.
         Para datos dinámicos (productos, precios) usar t-foreach sobre
         product.template en vez de tarjetas estáticas. -->
    <template id="s_cevende_solar_calculator" name="CEVENDE solar_calculator">
        <!-- Pendiente: ver preview/index.html, sección solar_calculator -->
    </template>
</odoo>
