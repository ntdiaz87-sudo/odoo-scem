<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- SNIPPET: solutions_grid
         NOTA CLAUDE CODE: portar el bloque 'solutions_grid' de preview/index.html
         a este snippet, con la MISMA estructura que snippets/hero_split.xml:
           1. <template id="s_cevende_solutions_grid"> con el HTML del bloque.
           2. Registro en website.snippets para hacerlo arrastrable.
         Para datos dinámicos (productos, precios) usar t-foreach sobre
         product.template en vez de tarjetas estáticas. -->
    <template id="s_cevende_solutions_grid" name="CEVENDE solutions_grid">
        <!-- Pendiente: ver preview/index.html, sección solutions_grid -->
    </template>
</odoo>
