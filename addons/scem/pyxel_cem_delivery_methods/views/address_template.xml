<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <template id="address_inherit" inherit_id="website_sale.address" name="Address Management">
            <form class="checkout_autoformat" position="replace">
                <form t-if="account_on_checkout != 'mandatory' or not is_public_user"
                      action="/shop/address" method="post" class="checkout_autoformat" position="replace">
                    <t t-if="disable_phone_autocomplete">
                        <script>
                            window.pyxelImportInstalled = true;
                        </script>
                    </t>
                    <div class="row">
                        <div t-attf-class="#{error.get('name') and 'o_has_error'} div_name col-lg-12 mb-2">
                            <label class="col-form-label" for="name">Nombre</label>
                            <input type="text" name="name"
                                   t-attf-class="form-control #{error.get('name') and 'is-invalid' or ''}"
                                   t-att-value="'name' in checkout and checkout['name']"/>
                        </div>
                        <div class="w-100"/>
                        <div t-attf-class="#{error.get('email') and 'o_has_error'} col-lg-6 mb-2" id="div_email">
                            <label t-attf-class="col-form-label" for="email">Correo Electrónico</label>
                            <input type="email" name="email"
                                   t-attf-class="form-control #{error.get('email') and 'is-invalid' or ''}"
                                   t-att-value="'email' in checkout and checkout['email']"/>
                        </div>

                        <t t-if="website._display_partner_b2b_fields()">
                            <div class="w-100"/>
                            <t t-set='vat_warning'
                               t-value="'vat' in checkout and checkout['vat'] and not can_edit_vat"/>
                            <t t-if="(mode == ('new', 'billing') and is_public_order
                                                or mode == ('edit', 'billing') and partner_id == website_sale_order.partner_id.id)
                                                and (can_edit_vat or 'vat' in checkout and checkout['vat'])"
                            >
                                <div t-attf-class="#{error.get('company_name') and 'o_has_error'} col-lg-6 mb-2">
                                    <label class="col-form-label fw-normal label-optional"
                                           for="company_name">Compañía
                                    </label>
                                    <input type="text" name="company_name"
                                           t-attf-class="form-control #{error.get('company_name') and 'is-invalid' or ''}"
                                           t-att-value="'commercial_company_name' in checkout and checkout['commercial_company_name'] or 'company_name' in checkout and checkout['company_name']"
                                           t-att-readonly="'1' if vat_warning else None"/>
                                    <small t-if="vat_warning"
                                           class="form-text text-muted d-block d-lg-none">Changing
                                        company name is not allowed once document(s) have been
                                        issued for your account. Please contact us directly for this
                                        operation.
                                    </small>
                                </div>
                                <div t-attf-class="#{error.get('vat') and 'o_has_error'} div_vat col-lg-6 mb-2">
                                    <label class="col-form-label fw-normal label-optional"
                                           for="vat">VAT
                                    </label>
                                    <input type="text" name="vat"
                                           t-attf-class="form-control #{error.get('vat') and 'is-invalid' or ''}"
                                           t-att-value="'vat' in checkout and checkout['vat']"
                                           t-att-readonly="'1' if vat_warning else None"/>
                                    <small t-if="vat_warning"
                                           class="form-text text-muted d-block d-lg-none">Changing
                                        VAT number is not allowed once document(s) have been issued
                                        for your account. Please contact us directly for this
                                        operation.
                                    </small>
                                </div>
                                <div t-if="vat_warning" class="col-12 d-none d-lg-block mb-1">
                                    <small class="form-text text-muted">Changing company name or VAT
                                        number is not allowed once document(s) have been issued for
                                        your account. Please contact us directly for this operation.
                                    </small>
                                </div>
                            </t>
                        </t>
                        <div t-attf-class="#{error.get('street') and 'o_has_error'} div_street col-lg-12 mb-2">
                            <label class="col-form-label" for="street">Calle y número</label>
                            <input type="text" name="street"
                                   t-attf-class="form-control #{error.get('street') and 'is-invalid' or ''}"
                                   t-att-value="'street' in checkout and checkout['street']"/>
                        </div>
                        <div t-attf-class="mb-2 #{error.get('street2') and 'o_has_error' or ''} col-lg-12 div_street2">
                            <label class="col-form-label label-optional" for="street2">Apartamento, edificio, etc.
                            </label>
                            <input type="text" name="street2"
                                   t-attf-class="form-control #{error.get('street2') and 'is-invalid' or ''}"
                                   t-att-value="'street2' in checkout and checkout['street2']"/>
                        </div>
                        <div class="w-100"/>
                        <t t-set='zip_city'
                           t-value='country and [x for x in country.get_address_fields() if x in ["zip", "city"]] or ["city", "zip"]'/>

                        <div class="w-100"/>


                        <t t-set="current_shipping_city_name"
                           t-value="request.session['current_city_name'] if 'current_city_name' in request.session else False"/>
                        <t t-set="current_shipping_municipality_id"
                           t-value="request.session['delivery_city'] if 'delivery_city' in request.session else False"/>
                        <t t-set="current_shipping_state_name"
                           t-value="request.session['current_state_name'] if 'current_state_name' in request.session else False"/>
                        <t t-set="current_shipping_state_id"
                           t-value="request.session['delivery_state'] if 'delivery_state' in request.session else False"/>
                        <t t-set="country_delivery_name"
                           t-value="request.session['country_delivery_name'] if 'country_delivery_name' in request.session else False"/>
                        <t t-set="country_delivery_id"
                           t-value="request.session['country_delivery_id'] if 'country_delivery_id' in request.session else False"/>

                        <input type="hidden" class="d-none" id="mode_address_action" t-att-value="mode[0]"
                               t-nocache="Trur"/>

                        <input type="hidden" class="d-none" id="mode_address_type" t-att-value="mode[1]"
                               t-nocache="True"/>

                        <t t-if="mode == ('new', 'shipping')">
                            <div t-attf-class="#{error.get('country_id') and 'o_has_error'} div_country col-lg-6 mb-2">
                                <label class="col-form-label" for="country_id">País</label>
                                <select id="country_id" name="country_id"
                                        t-attf-class="form-select #{error.get('country_id') and 'is-invalid' or ''}"
                                        t-att-mode="mode[1]">
                                    <option value="">País</option>
                                    <t t-foreach="countries" t-as="c">
                                        <option t-att-value="c.id"
                                                t-att-selected="c.id == (country and country.id or -1)"
                                                t-att-data-country-code="c.code == 'CU' and 'CU' or None">
                                            <t t-esc="c.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <div t-attf-class="#{error.get('state_id') and 'o_has_error'} div_state col-lg-6 mb-2"
                                 t-att-style="(not country or not country.state_ids) and 'display: none'">
                                <label class="col-form-label" for="state_id">Provincia</label>
                                <select name="state_id"
                                        t-attf-class="form-select #{error.get('state_id') and 'is-invalid' or ''}"
                                        data-init="1">
                                    <option t-att-value="current_shipping_state_id"
                                            t-att-selected="True">
                                        <t t-esc="current_shipping_state_name"/>
                                    </option>

                                </select>
                            </div>

                            <div t-attf-class="#{error.get('municipality_id') and 'o_has_error'} div_city_select col-lg-6 mb-2"
                                 t-att-style="(not country or not country.state_ids) and 'display: none'">
                                <label class="col-form-label" for="state_id">Municipio</label>
                                <select name="municipality_id"
                                        t-attf-class="form-select #{error.get('municipality_id') and 'is-invalid' or ''}"
                                        data-init="1">

                                    <option t-att-value="current_shipping_municipality_id"
                                            t-att-selected="True">
                                        <t t-esc="current_shipping_city_name"/>
                                    </option>

                                </select>
                            </div>


                            <div t-attf-class="#{error.get('city') and 'o_has_error' or ''} d-none div_city col-md-8 mb-2">
                                <label class="col-form-label" for="city">Municipio</label>
                                <input type="text" name="city"
                                       t-attf-class="form-control #{error.get('city') and 'is-invalid' or ''}"
                                       t-att-value="current_shipping_city_name"/>
                            </div>

                        </t>

                        <t t-elif="mode == ('edit', 'shipping')">
                            <div t-attf-class="#{error.get('country_id') and 'o_has_error'} div_country col-lg-6 mb-2">
                                <label class="col-form-label" for="country_id">País</label>
                                <select id="country_id" name="country_id"
                                        t-attf-class="form-select #{error.get('country_id') and 'is-invalid' or ''}"
                                        t-att-mode="mode[1]">
                                    <option value="">País...</option>
                                    <t t-foreach="countries" t-as="c">
                                        <option t-att-value="c.id"
                                                t-att-selected="c.id == (country and country.id or -1)"
                                                t-att-data-country-code="c.code == 'CU' and 'CU' or None">
                                            <t t-esc="c.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <div t-attf-class="#{error.get('state_id') and 'o_has_error'} div_state col-lg-6 mb-2"
                                 t-att-style="(not country or not country.state_ids) and 'display: none'">
                                <label class="col-form-label" for="state_id">Provincia</label>
                                <select name="state_id"
                                        t-attf-class="form-select #{error.get('state_id') and 'is-invalid' or ''}"
                                        data-init="1">
                                    <option t-att-value="current_shipping_state_id"
                                            t-att-selected="True">
                                        <t t-esc="current_shipping_state_name"/>
                                    </option>
                                </select>
                            </div>
                            <div t-attf-class="#{error.get('municipality_id') and 'o_has_error'} div_city_select col-lg-6 mb-2 "
                                 t-att-style="(not country or not country.state_ids) and 'display: none'">
                                <label class="col-form-label" for="state_id">Municipio</label>
                                <select name="municipality_id"
                                        t-attf-class="form-select #{error.get('municipality_id') and 'is-invalid' or ''}"
                                        data-init="1">


                                    <option t-att-value="current_shipping_municipality_id"
                                            t-att-selected="True">
                                        <t t-esc="current_shipping_city_name"/>
                                    </option>

                                </select>
                            </div>
                            <div t-attf-class="#{error.get('city') and 'o_has_error' or ''} d-none div_city col-md-8 mb-2">
                                <label class="col-form-label" for="city">Municipio</label>
                                <input type="text" name="city"
                                       t-attf-class="form-control #{error.get('city') and 'is-invalid' or ''}"
                                       t-att-value="current_shipping_city_name"/>
                            </div>
                        </t>
                        <t t-else="">
                            <div t-attf-class="#{error.get('country_id') and 'o_has_error'} div_country col-lg-6 mb-2">
                                <label class="col-form-label" for="country_id">País</label>
                                <select id="country_id" name="country_id"
                                        t-attf-class="form-select #{error.get('country_id') and 'is-invalid' or ''}"
                                        t-att-mode="mode[1]">
                                    <option value="">País...</option>
                                    <t t-foreach="countries" t-as="c">
                                        <option t-att-value="c.id"
                                                t-att-selected="c.id == (country and country.id or -1)"
                                                t-att-data-country-code="c.code == 'CU' and 'CU' or None">
                                            <t t-esc="c.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <div t-attf-class="#{error.get('state_id') and 'o_has_error'} div_state col-lg-6 mb-2"
                                 t-att-style="(not country or not country.state_ids) and 'display: none'">
                                <label class="col-form-label" for="state_id">Provincia</label>
                                <select name="state_id"
                                        t-attf-class="form-select #{error.get('state_id') and 'is-invalid' or ''}"
                                        data-init="1">
                                    <option value="">Provincia...</option>
                                    <t t-foreach="country_states" t-as="s">
                                        <option t-att-value="s.id"
                                                t-att-selected="s.id == ('state_id' in checkout and country and checkout['state_id'] != '' and int(checkout['state_id']))">
                                            <t t-esc="s.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <t t-set="current_shipping_city"
                               t-value="request.session['current_city_name'] if 'current_city_name' in request.session else False"/>

                            <t t-if="country and country.code == 'CU'">
                                <div t-attf-class="#{error.get('municipality_id') and 'o_has_error'} div_city_select col-lg-6 mb-2"
                                     t-att-style="(not country or not country.state_ids) and 'display: none'">
                                    <label class="col-form-label" for="state_id">Municipio</label>
                                    <select name="municipality_id"
                                            t-attf-class="form-select #{error.get('municipality_id') and 'is-invalid' or ''}"
                                            data-init="1">
                                        <option value="">Municipio...</option>
                                        <t t-foreach="cities" t-as="s">
                                            <option t-att-value="s.id"
                                                    t-att-selected="
                                                        ( 'municipality_id' in checkout
                                                        and checkout['municipality_id'] != ''
                                                        and int(checkout['municipality_id']) == s.id )
                                                        or
                                                        ( not ('municipality_id' in checkout)
                                                        and current_shipping_municipality_id
                                                        and current_shipping_municipality_id == s.id )
                                                    ">
                                                <t t-esc="s.name"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>

                                <!-- Para Cuba, ocultamos el input de texto; lo dejamos solo por compatibilidad -->
                                <div t-attf-class="#{error.get('city') and 'o_has_error' or ''} div_city col-md-8 mb-2 d-none">
                                    <label class="col-form-label" for="city">Municipio</label>
                                    <input type="text" name="city"
                                           t-attf-class="form-control #{error.get('city') and 'is-invalid' or ''}"
                                           t-att-value="'city' in checkout and checkout['city']"/>
                                </div>
                            </t>

                            <!-- Para otros países: comportamiento estándar de Odoo -->
                            <t t-else="">
                                <div t-attf-class="#{error.get('municipality_id') and 'o_has_error'} div_city_select col-lg-6 mb-2 d-none"
                                     t-att-style="(not country or not country.state_ids) and 'display: none'">
                                    <label class="col-form-label" for="state_id">Municipio</label>
                                    <select name="municipality_id"
                                            t-attf-class="form-select #{error.get('municipality_id') and 'is-invalid' or ''}"
                                            data-init="1">
                                        <option value="">Municipio...</option>
                                        <t t-foreach="cities" t-as="s">
                                            <option t-att-value="s.id"
                                                    t-att-selected="s.id == ('municipality_id' in checkout and country and checkout['municipality_id'] != '' and int(checkout['municipality_id']))">
                                                <t t-esc="s.name"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>
                                <div t-attf-class="#{error.get('city') and 'o_has_error' or ''} div_city col-md-8 mb-2">
                                    <label class="col-form-label" for="city">Municipio</label>
                                    <input type="text" name="city"
                                           t-attf-class="form-control #{error.get('city') and 'is-invalid' or ''}"
                                           t-att-value="'city' in checkout and checkout['city']"/>
                                </div>
                            </t>
                        </t>

                        <t t-if="'zip' in zip_city and zip_city.index('zip') &lt; zip_city.index('city')">
                            <div t-attf-class="#{error.get('zip') and 'o_has_error'} div_zip col-md-4 mb-2">
                                <label class="col-form-label label-optional" for="zip">Código Postal
                                </label>
                                <input type="text" name="zip"
                                       t-attf-class="form-control #{error.get('zip') and 'is-invalid' or ''}"
                                       t-att-value="'zip' in checkout and checkout['zip']"/>
                            </div>
                        </t>

                        <t t-if="'zip' in zip_city and zip_city.index('zip') &gt; zip_city.index('city')">
                            <div t-attf-class="#{error.get('zip') and 'o_has_error'} div_zip col-md-4 mb-2">
                                <label class="col-form-label label-optional" for="zip">Código Postal
                                </label>
                                <input type="text" name="zip"
                                       t-attf-class="form-control #{error.get('zip') and 'is-invalid' or ''}"
                                       t-att-value="'zip' in checkout and checkout['zip']"/>
                            </div>
                        </t>

                        <div t-attf-class="#{error.get('phone') and 'o_has_error'} col-lg-6 mb-2" id="div_phone">
                            <label class="col-form-label" for="phone">Teléfono</label>
                            <input id="phone" type="tel" name="phone"
                                   t-attf-class="form-control #{error.get('phone') and 'is-invalid' or ''}"
                                   t-att-value="'phone' in checkout and checkout['phone']"
                                   t-att-data-country-code="country and country.code == 'CU' and (not disable_phone_autocomplete and '53' or '') or ''"
                                   t-att-placeholder="country and country.code == 'CU' and (not disable_phone_autocomplete and '+53' or 'Teléfono') or '+1'"
                                   t-att-autocomplete="disable_phone_autocomplete and 'off' or 'on'"
                                   t-att-readonly="disable_phone_autocomplete and False or None"/>
                            <!-- Si el módulo está instalado, mostramos un texto indicando que no se autocompleta -->
                            <!--<small t-if="disable_phone_autocomplete" class="form-text text-muted">
                                Ingrese el número de teléfono exactamente como aparece en su cuenta
                            </small>-->
                        </div>

                        <div class="w-100"/>
                        <t t-if="mode == ('new', 'billing') and not only_services">
                            <div class="col-lg-12">
                                <div class="form-check form-switch mt-2 mb-3">
                                    <label>
                                        <input
                                                type="checkbox"
                                                id="shipping_use_same"
                                                class="form-check-input mr8"
                                                name="use_same"
                                                value="1"
                                                t-att-checked="use_same"/>Enviar a la misma dirección
                                        <span
                                                t-if="is_public_user"
                                                class="form-check-label ship_to_other text-muted"
                                                style="display: none">
                                            &amp;nbsp;(
                                            <i>Debes llenar después la dirección de envío)</i>
                                        </span>
                                    </label>
                                </div>
                            </div>
                        </t>
                        <t t-else="">
                            <input type="hidden" name="use_same"
                                   t-att-value="partner_id == website_sale_order.partner_shipping_id.id == website_sale_order.partner_invoice_id.id"/>
                        </t>
                    </div>

                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"
                           t-nocache="The csrf token must always be up to date."/>
                    <input type="hidden" name="submitted" value="1"/>
                    <input type="hidden" name="partner_id" t-att-value="partner_id or '0'"/>
                    <input type="hidden" name="mode" t-att-value="mode[1]"/>
                    <input type="hidden" name="callback" t-att-value="callback"/>

                    <input type="hidden" name="field_required" t-att-value="'name,street'"/>

                    <div class="d-flex flex-column flex-md-row align-items-center justify-content-between mt32 mb32">
                        <a role="button"
                           t-att-href="mode == ('new', 'billing') and '/shop/cart' or '/shop/checkout'"
                           class="btn btn-outline-secondary w-100 w-md-auto order-md-1 order-3">
                            <i class="fw-light fa fa-angle-left me-2"/>Descartar
                        </a>
                        <div class="position-relative w-100 d-flex d-md-none justify-content-center align-items-center order-2 my-2 opacity-75">
                            <hr class="w-100"/>
                            <span class="px-3">or</span>
                            <hr class="w-100"/>
                        </div>
                        <a role="button" href="#"
                           class="a-submit a-submit-disable a-submit-loading btn btn-primary w-100 w-md-auto order-1 order-md-3">
                            <t t-if="mode == ('new', 'billing')">
                                Continuar al checkout
                            </t>
                            <t t-else="">
                                Guardar
                            </t>
                            <i class="fw-light fa fa-angle-right ms-2"/>
                        </a>
                    </div>
                </form>
            </form>

        </template>

        <template id="address_kanban" name="Kanban address">
            <t t-set="mode" t-value="is_invoice and 'billing' or 'shipping'"/>
            <form action="/shop/cart/update_address" method="POST" class="d-none">
                <input type="hidden"
                       name="csrf_token"
                       t-att-value="request.csrf_token()"
                       t-nocache="The csrf token must always be up to date."/>
                <input type="hidden" name="partner_id" t-att-value="contact.id"/>
                <input type="hidden" name="mode" t-att-value="mode"/>
                <input type="submit"/>
            </form>
            <div t-attf-class="card position-relative h-100 #{selected and 'bg-primary border border-primary' or is_invoice and 'js_change_billing' or ('js_change_shipping' if is_available_to_selected else '')}">
                <div class="card-body d-flex flex-column align-items-start">
                    <t t-esc="contact" t-options="dict(widget='contact', fields=['name', 'address'], no_marker=True)"/>
                    <t t-if="contact._can_be_edited_by_current_customer(website_sale_order, mode)">
                        <t t-set="new_address_href" t-value="'/shop/address?mode=' + mode"/>
                        <a
                                t-att-href="new_address_href + '&amp;partner_id=' + str(contact.id)"
                                class="js_edit_address btn btn-link p-0 mt-auto"
                                role="button"
                                title="Edit this address"
                                aria-label="Edit this address">
                            <i class="fa fa-pencil me-1"/>Editar
                        </a>
                    </t>
                </div>
            </div>
        </template>

        <template id="row_addresses">
            <div t-attf-class="{{'all_billing' if is_invoice else 'all_shipping'}} row row-cols-md-2 row-cols-lg-3 g-3 flex-nowrap flex-md-wrap mb32">
                <t t-set="current_shipping_municipality_id"
                   t-value="request.session['delivery_city'] if 'delivery_city' in request.session else False"/>
                <div t-foreach="addresses.filtered(lambda a: current_shipping_municipality_id == a.municipality_id.id or is_invoice)"
                     t-as="addr"
                     class="one_kanban col-md">

                    <t t-call="pyxel_cem_delivery_methods.address_kanban">
                        <t t-set="contact" t-value="addr"/>
                        <t t-set="selected" t-value="len(addresses) == 1 or addr == selected_address"/>
                        <t t-set="is_available_to_selected"
                           t-value="is_invoice or current_shipping_municipality_id == addr.municipality_id.id"/>
                    </t>
                </div>
                <div t-if="not is_invoice or not order.website_id.is_public_user()" class="one_kanban col-md">
                    <t t-if="is_invoice">
                        <t t-set="new_address_href" t-valuef="/shop/address?mode=billing"/>
                    </t>
                    <t t-else="">
                        <t t-set="new_address_href" t-valuef="/shop/address?mode=shipping"/>
                    </t>
                    <a role="button" t-att-href="new_address_href"
                       class="o_wsale_add_address d-flex align-items-center justify-content-center h-100 px-4 border rounded mx-auto no-decoration">
                        <i class="fa fa-plus me-md-2"/>
                        <span class="d-none d-md-inline">Añadir dirección</span>
                    </a>
                </div>
            </div>
        </template>


        <template id="checkout" inherit_id="website_sale.checkout">

            <t t-if="not only_services" position="replace">
                <t t-if="not only_services" groups="account.group_delivery_invoice_address">
                    <div class="row">
                        <div class="col-lg-12">
                            <h4>Direcciones de Entrega</h4>
                        </div>
                    </div>
                    <t t-call="pyxel_cem_delivery_methods.row_addresses">
                        <t t-set="order" t-value="order"/>
                        <t t-set="is_invoice" t-value="False"/>
                        <t t-set="addresses" t-value="shippings"/>
                        <t t-set="selected_address" t-value="order.partner_shipping_id"/>
                        <t t-set="invalid_address" t-value="request.context.get('invalid_address', False)"/>
                    </t>
                </t>
            </t>

        </template>


    </data>


</odoo> 