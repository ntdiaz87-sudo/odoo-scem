<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="mail_template_sale_canceled" model="mail.template">
            <field name="name">CEM Sales: Order Canceled Confirmation</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">{{ object.company_id.name }}: Order Canceled {{ object.name or 'n/a' }} / Invoice canceled too</field>
            <field name="email_from">{{ (object.company_id or object.company_id.partner_id or user).email_formatted }}</field>
            <field name="email_to">{{ object.partner_id.email_formatted }}</field>
            <field name="description">Sent to customers when order has been canceled</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 12px;">
                        Dear
                        <t t-out="object.partner_id.name or ''">Brandon Freeman</t>.
                        <br/>
                        <br/>
                        Your order
                        <span style="font-weight:bold;" t-out="object.name or ''">SUB003</span>
                        amounting in
                        <span style="font-weight:bold;"
                              t-out="format_amount(object.amount_total, object.currency_id) or ''">$ 143,750.00
                        </span>
                        from <t t-out="object.company_id.name or ''">YourCompany</t> has been canceled.
                        <br/>
                        The invoice has been canceled too.

                    </p>
                </div>
            </field>
            <field name="report_template_ids" eval="[(4, ref('sale.action_report_saleorder'))]"/>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="True"/>
        </record>

    </data>
</odoo>
