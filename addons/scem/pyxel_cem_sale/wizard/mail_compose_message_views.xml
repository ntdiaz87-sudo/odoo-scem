<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <!-- Add mass mail campaign to the mail.compose.message form view -->
        <record model="ir.ui.view" id="email_compose_form_mass_mailing_inherit">
            <field name="name">mail.compose.message.form.mass_mailing.cem</field>
            <field name="model">mail.compose.message</field>
            <field name="inherit_id" ref="mail.email_compose_message_wizard_form"/>
            <field name="arch" type="xml">
                <button class="btn-secondary" special="cancel" position="attributes">
                    <attribute name="invisible">context.get('cem_mailing')</attribute>
                </button>
                <xpath expr="//notebook//field[@name='template_id']" position="replace"/>
            </field>
        </record>

</odoo>
